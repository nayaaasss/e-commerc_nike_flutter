import 'package:flutter/material.dart';

// A primary color definition
const primaryColor = Color(0xFFFF7643);
const secondColor = Color(0xFF0D6EFD);
const primaryLightColor = Color(0xFFFFECDF);
const primaryDeskColor = Color.fromARGB(255, 255, 255, 255);
const primaryGradientColor = LinearGradient(
  colors: [
    Color(0xFFFFA53E),
    Color(0xFFFF7643),
  ]);

// A seceondary color definition
const secondaryColor = Color(0xFF979797);
const textColor = Color(0xFF757575);

// animationDuration definition
const animationDuration = Duration(
  milliseconds: 200
);

//default value constant value for padding size
const defaultPadding = 20.0;